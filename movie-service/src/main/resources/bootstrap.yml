spring:
   profiles:
      active: dev
      
---     

spring:
  cloud:
    config:
      uri: http://localhost:8256
      fail-fast: false


---
spring:
   profiles: dev
   cloud:   
      config: 
         uri: http://localhost:8200
         fail-fast: false